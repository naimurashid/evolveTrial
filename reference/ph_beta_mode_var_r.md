# Compute mode and variance of log-HR posterior (ORIGINAL R VERSION)

Compute mode and variance of log-HR posterior (ORIGINAL R VERSION)

## Usage

``` r
ph_beta_mode_var_r(
  E_C,
  PT_C,
  E_T,
  PT_T,
  alpha_prior,
  beta_prior,
  mu,
  sigma,
  tol = 1e-06,
  max_iter = 50
)
```
