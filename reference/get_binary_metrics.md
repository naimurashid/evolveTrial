# Get binary trial metrics from registry

Get binary trial metrics from registry

## Usage

``` r
get_binary_metrics(registry_df, stage = NULL)
```

## Arguments

- registry_df:

  Binary registry data frame

- stage:

  Optional: filter to specific stage (1, 2, or NULL for all)

## Value

List with n_enrolled, n_responses
