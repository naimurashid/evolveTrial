# Create state container for binary endpoint trial

Create state container for binary endpoint trial

## Usage

``` r
make_state_binary(arm_names, max_total_patients_per_arm)
```

## Arguments

- arm_names:

  Character vector of arm names

- max_total_patients_per_arm:

  Named integer vector of max N per arm

## Value

State list for binary trial simulation
