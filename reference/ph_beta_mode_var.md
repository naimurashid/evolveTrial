# Compute mode and variance of log-HR posterior for PH model (DISPATCHER)

Compute mode and variance of log-HR posterior for PH model (DISPATCHER)

## Usage

``` r
ph_beta_mode_var(
  E_C,
  PT_C,
  E_T,
  PT_T,
  alpha_prior,
  beta_prior,
  mu,
  sigma,
  tol = 1e-06,
  max_iter = 50
)
```
