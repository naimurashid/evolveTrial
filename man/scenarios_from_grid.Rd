% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_driver.R
\name{scenarios_from_grid}
\alias{scenarios_from_grid}
\title{Build scenario overrides from a grid of design choices}
\usage{
scenarios_from_grid(choices)
}
\arguments{
\item{choices}{Named list where each element is either a vector of scalar
values or a list whose elements are per-arm vectors.}
}
\value{
A list of scenario override lists.  The underlying Cartesian grid is
attached as an attribute named \code{"grid"}.
}
\description{
Expands a named list of options into a list of scenario override lists that
can be merged with \code{base_args} prior to simulation.
}
\examples{
scenarios_from_grid(list(
  max_total_patients_per_arm = list(
    c(Doublet = 60, Triplet = 60),
    c(Doublet = 60, Triplet = 70)
  ),
  compare_arms_futility_margin = c(0.3, 0.4)
))

}
