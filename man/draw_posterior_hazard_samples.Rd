% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_cpp_dispatchers.R,
%   R/posterior_helpers.R
\name{draw_posterior_hazard_samples}
\alias{draw_posterior_hazard_samples}
\title{Draw posterior hazard samples (DISPATCHER)}
\usage{
draw_posterior_hazard_samples(
  num_intervals,
  events_per_interval,
  person_time_per_interval,
  prior_alpha_params,
  prior_beta_params,
  num_samples = 1000
)

draw_posterior_hazard_samples(
  num_intervals,
  events_per_interval,
  person_time_per_interval,
  prior_alpha_params,
  prior_beta_params,
  num_samples = 1000
)
}
\arguments{
\item{num_intervals}{Number of intervals in the piecewise model.}

\item{events_per_interval}{Integer vector of observed events per interval.}

\item{person_time_per_interval}{Numeric vector of person-time at risk per interval.}

\item{prior_alpha_params}{Numeric vector of Gamma prior shape parameters.}

\item{prior_beta_params}{Numeric vector of Gamma prior rate parameters.}

\item{num_samples}{Number of posterior samples to draw (default 1000).}
}
\value{
Matrix of posterior hazard samples

Matrix with \code{num_samples} rows and \code{num_intervals} columns of hazard samples.
}
\description{
Routes to C++ or R implementation based on EVOLVETRIAL_USE_CPP.
Default: C++ for better performance.

Draws samples from the posterior distribution of hazard rates for each interval
in a Bayesian piecewise exponential model with Gamma priors.
}
\keyword{internal}
